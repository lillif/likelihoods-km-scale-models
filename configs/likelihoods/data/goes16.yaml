model_key: goes16
data_dir: ${oc.env:DATAROOT}/goes/patches64_stride32/
npz_image_key: olr

time: # null = use all years / months / days available
  years: [2024]
  months: null
  days: null

dataloader:
  batch_size: 128
  num_workers: 16
  pin_memory: true
  prefetch_factor: 2
